version: "3.9"
services:
  chatbot:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - backend/.env
    depends_on:
      - redis
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    restart: unless-stopped
